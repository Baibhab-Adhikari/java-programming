4. Define the 3 classes named Add, Sub,  Div with two member variables each amd get methods to initialize the variables and add(), sub() and div() in respective classes. All the classes have show() method to display the result of addition, subtraction, and division results respectively. Define Main class that have main() method. In main() method, create objects of three classes and perform arithmetic operations. The variables are initialized with 12 and 6 respectively.

5. Define a base class Animal, with type as member variable that imply domestic or wild animal. Initialize the variable with get() method. Define a derived class Cat which has two member variables breed and colour. Initialize the variables with set() method. Define show() method in Cat class that show the attributes of a Cat. Define a Main class and create two objects of the Cat class.